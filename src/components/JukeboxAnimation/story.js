import React from 'react';
import { storiesOf } from '@storybook/react';
import JukeboxAnimation from '.';

storiesOf('Jukebox Animation Component', module).add('Jukebox', () => (
  <JukeboxAnimation />
));
